import './Output.css';

export default function Output() {
    return (
      <div>
        <p>Number of characters (including spaces):</p>
        <p>Number of characters (without spaces):</p>
        <p>Number of words:</p>
        <p>Number of sentences:</p>
        <p>Number of syllables:</p>
      </div>
    );
  }